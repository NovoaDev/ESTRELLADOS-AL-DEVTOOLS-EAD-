AL
TEST!
Consumir WS
Dimensiones
Correo
tfieldpageconcaption y coment solo sin ES

DOCKER
Crear contenedor SQL externo
Extraer codigo de las apis
        Get-NavContainerAppInfo `
            -containerName $TESTServer 
------------------------------------------------

HTTP para probar API'S
get todos
post insert
post filtrado
delete
update

JSON
Settings.json

SQL
Agregar DB en un contenedor ya creado.

Extensiones extras
- script para hacer merge de manera mas eficiente que suba solo todo lo firmado (Merge-NAVApplicationObject).
- Boton en vscode como el de git de arriba que abra la url dle contendedor
        Start-Process "http://$($DEVServer):8080/"
        ----------------------------------------

    start $TESTmainPageUrl

    -------------------------------------------------------------------------------------------------
    Traer campos de configuracion desde paginas de configuracion si esta marcado con un booll